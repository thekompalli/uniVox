"""
Lightweight model stubs used for local development.

These implementations avoid heavyweight downloads and let the API start
even when full ML model files are not present. Production deployments
should replace these with real inference wrappers.
"""

